// Generated by CoffeeScript 1.7.1
(function() {
  define(function() {
    var build, randomStar, starField;
    randomStar = function(minDist, maxDist) {
      var phi, r, theta;
      r = THREE.Math.randFloat(minDist, maxDist);
      theta = THREE.Math.randFloat(0, 2 * Math.PI);
      phi = THREE.Math.randFloat(0, 2 * Math.PI);
      return new THREE.Vector3(r * Math.sin(theta) * Math.cos(phi), r * Math.sin(theta) * Math.sin(phi), r * Math.cos(theta));
    };
    starField = function(min, max, n) {
      var geometry, material;
      geometry = new THREE.Geometry();
      while (n -= 1) {
        geometry.vertices.push(randomStar(min, max));
      }
      material = new THREE.PointCloudMaterial({
        color: 0xffffff,
        size: 0.1
      });
      return new THREE.PointCloud(geometry, material);
    };
    build = function(scene, settings) {
      return scene.add(starField(750, 1000, settings.num_stars));
    };
    return build;
  });

}).call(this);

//# sourceMappingURL=starField.map
